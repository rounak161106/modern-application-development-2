1. With routes, we can navigate a component with a route and whenever the user navigates to that route, that particular component is shown. Eg. profile component -> /profile route
           about component -> /about route

2. In the initial weeks, we had defined a component in the script.js and this was globally available in the app and we could use this component anywhere in the app and as many times but now we will define routes and map component to routes and we can navigate and see the component it is mapped to.
