// used as a configuration file for Babel in a Vue.js project created with Vue CLI. It specifies the presets that Babel should use to transpile the JavaScript code. In this case, it uses the '@vue/cli-plugin-babel/preset' preset, which includes the necessary plugins and configurations for a Vue.js application.

module.exports = {
  presets: [
    '@vue/cli-plugin-babel/preset'
  ]
}
